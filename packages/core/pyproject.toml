[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = 'ili-core'
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ['argon2-cffi>=25,<26', 'pydantic>=2.10,<3']

[tool.setuptools.packages.find]
where = ["src"]
