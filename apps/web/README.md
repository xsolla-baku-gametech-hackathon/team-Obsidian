# Frontend

React + TypeScript dashboard. Feature folders own their views and hooks; `components`
holds shared UI. `lib/api` owns generated wire types and interchangeable HTTP/snapshot
adapters. No scoring or source ingestion in the browser.

This is a directory scaffold; package manifests and runnable code are pending. See
[architecture](../../docs/ARCHITECTURE.md) and [contracts](../../contracts/README.md).
