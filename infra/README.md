# Deployment

Reserved for deployment configuration once the application is runnable. MVP needs
static frontend assets plus a Python API/job host with persistent local SQLite storage.
Keep environment secrets outside Git. Introduce containers and managed services only
when required by the chosen hosting environment.
